## Vite only exposes variables prefixed with VITE_.
# Leave unset for local Vite dev so `/api` uses the Vite proxy.
# Set this in deployments where the API is on a different origin.
# VITE_API_BASE_URL=https://api.example.com
